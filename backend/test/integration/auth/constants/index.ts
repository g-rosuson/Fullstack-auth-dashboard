/**
 * Password that satisfies the production register schema (length, mixed case, digit, symbol).
 * Paired with {@link buildRegisterPayload} for integration registrations.
 */
const INTEGRATION_AUTH_PASSWORD = 'SecureP@ss1';

export { INTEGRATION_AUTH_PASSWORD };
