# Avatar

A user identity control shows a glyph derived from the user's identity.

- **FR-UI-AVR-001** — The system shall expose a user identity control whose visible glyph is derived from the user's identity.
