# Name uniqueness

Per-user job name uniqueness on create and rename.

- **FR-JOBS-UNQ-001** — Job names shall be unique per user. Creating or renaming a job to a name that user already has shall be rejected. Different users may share the same name.
