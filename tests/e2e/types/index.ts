/**
 * A registered test user.
 */
type RegisteredTestUser = {
    email: string;
    password: string;
    firstName: string;
    lastName: string;
    accessToken: string;
};

export type { RegisteredTestUser };
